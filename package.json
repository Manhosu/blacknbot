{
  "name": "blackinbot",
  "version": "1.0.0",
  "description": "BlackinBot - Sistema de gerenciamento de bots Telegram",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm run start",
    "backend": "cd backend && python main.py"
  },
  "dependencies": {
    "axios": "^1.9.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
